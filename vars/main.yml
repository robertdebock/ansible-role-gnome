---
# vars file for gnome

_gnome_packages:
  default: []
  Alpine:
    - gnome-session
  Debian:
    - gnome-session
  Fedora:
    - "@Fedora Workstation"
    - "@Fonts"
    - dbus-x11
  RedHat:
    - "@Server with GUI"
  openSUSE Leap:
    - gnome-desktop
    - gnome-session

gnome_packages: "{{ _gnome_packages[ansible_distribution] | default(_gnome_packages[ansible_os_family] | default(_gnome_packages['default'] )) }}"

gnome_exclude:
  - "{{ ansible_distribution | lower }}-release-workstation"
  - "{{ ansible_distribution | lower }}-release-workstation"

_gnome_allowerasing:
  default: yes
  2.9: null

gnome_allowerasing: "{{ _gnome_allowerasing[ansible_version.major ~ '.' ~ ansible_version.minor] | default(_gnome_allowerasing['default']) }}"
